language:
  - elixir

elixir:
  - 1.3.2

opt_release:
  - 18.2

sudo:
  - required

services:
  - docker

before_install:
  - docker pull elasticsearch
  - docker run -d -p 9200:9200 elasticsearch
  - docker ps

before_script:
  - sleep 60

script:
  - docker ps
  - mix test
